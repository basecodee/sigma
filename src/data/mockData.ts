// This file is kept for reference only
// All data is now fetched from the backend

export const mockUnitKerjaData = [];
export const mockEdcData = [];

export const getUnitKerjaDataByYear = (year: number) => {
  console.warn('Mock data functions are deprecated. Data should be fetched from backend.');
  return [];
};

export const getEdcDataByYear = (year: number) => {
  console.warn('Mock data functions are deprecated. Data should be fetched from backend.');
  return [];
};